doctype html
html
    head
        title Express-app 3
        script(src='index.js' defer)
    body
        div(id="form") 
            label(for="navn") Navn
            input(id="navn" type="text")
            label(for="adresse") Adresse
            input(id="adresse" type="text") 

            button Tilføj person
        h2 Personer
        if personer && personer.length > 0
            list
                each person in personer
                    li #{person.name}, #{person.address}
        else 
            p Der er ingen personer.

    style.
        #form {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }